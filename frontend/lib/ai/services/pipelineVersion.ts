/**
 * Bump when pipeline semantics change so cached diagram/repo results are invalidated.
 * Single source of truth shared by diagramCache.ts and repoDiagramRedisCache.ts.
 */
export const PIPELINE_VERSION = 'v7';
